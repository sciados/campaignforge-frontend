// src/app/jwlinux/layout.js
export const metadata = {
  title: 'JW Library for Linux - Free Installation Tool | Rodgers Digital',
  description: 'Install JW Library on any Linux distribution with our automated installer.'
};

export default function Layout({ children }) {
  return children;
}